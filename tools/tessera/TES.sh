#!/bin/bash

#Auf den Task Scripting schient eine Malware aktiv zu sein, die das kurzzeit-gedächtniss alle 1 sek löscht, teilweile interaktiv zur FTT "das ist das problem was behoben werden muss"
#
#Dann weiteres scheint zu sein, dass es irgendwelche klopfgeräsuche in richtgung wand hinter meinem sessel zu geben

#dann scheint es irgend eine "instant verkrustungs-funktion" meiner nasen innenseite zu geben die grad aktiviert wurde

#dann das geräusch scheint irgend ein holrauum zu sein wo mutmasslich die geistteskranke Tremp jetzt schienbar von jemandem einen Terror-Afurrga bekommen hat


echo TES.sh srcFILE targetFILE subDir

subDir="$(echo $3|cut -c3-)"
echo SUBDIR: $subDir

mkdir -p "$(dirname "${2}$subDir}")"

echo EE tesseract $1 $2$subDir
#echo tesseract $1 $1

