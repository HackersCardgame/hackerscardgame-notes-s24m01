oder ein eferenztyp (eine Klasse oder Interface).
Optional kann eine Variable bei Deklaration durch einen Ausdruck zur Laufzeit mit einem Wert initialisiert werden.

Mit dem ebenfalls optionalen Schlusselwort final wird verhindert, dass der Wert einer Variablen nach dessen Deklaration geandert werden kann; lediglich eine erstmalige Zuweisu
Instanzvariablen ist méglich

Die Syntax einer Variablendeklaration’
[ final 1 [private|protected|public] Datentyp VARTABLENNAME [ = Ausdruck ] ;

zum Beispiel:

public class Beispiel {

static {
int i=0
// etwas Sinrvolles mit i anstellen

public void machwas() {
ong grosseGanzeZahl;
String name;
final float PI = 3.1415;
boolean [] wahrheitswerte = new boolean(10] ;
// etwas Sinnvolles mit grosseGanzeZahl, name und PI anstellen
// Die Zuweisung PI = 1; verursacht einen Conpilerfehler
// €in Zugriff auf i verursacht einen Compiler fehler
for (int i-0; i < wahrheitswerte.length; i++) {

// hier existiert i (ein anderes als obiges im static Block)

Sichtbarkeits- und Zugriffsmodifizierer [Bearbeiten |

Die drei Schllisselwérter private , protected und public dienen der Modifikation der Sichtbarkeit und des Zugriffs

private erlaubt nur der einenen Klasse den Juarife
