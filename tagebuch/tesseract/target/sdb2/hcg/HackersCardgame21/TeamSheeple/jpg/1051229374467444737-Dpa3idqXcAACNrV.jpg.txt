fn main() {
let v1
let v2

Vec'[t52;3,4, 5]:
vec!(1,2,3]5

println!("{:?}", longer_vector(&v1, &v2));
I

fn Longer_vector<'a>(x: &'a[i32], y: &'a[i32]) -> &'a[i32]
if x.len() > y.len { x } else { y }
